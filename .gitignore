__pycache__/
*.py[cod]
.DS_Store
.claude/
node_modules/
pw-browsers/
bin/
# `go build ./...` writes a binary named after the package directory, which is
# not what the Makefile builds and is not something to commit.
taskhound/taskhound
.taskhound.yaml.lock
dist/
